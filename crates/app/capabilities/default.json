{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The window may talk to this app's own commands, and open an external link in the system browser. Nothing else: no filesystem, no shell, no arbitrary HTTP from the page.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:allow-open-url"
  ]
}
